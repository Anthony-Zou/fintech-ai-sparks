#!/usr/bin/env python3"""Quick end-to-end test to verify core functionality."""import sysfrom pathlib import Path# Add parent directory to path for importssys.path.insert(0, str(Path(__file__).parent.parent))from core.trading_engine import TradingEngine, OrderSide, OrderTypefrom core.order_book import OrderBookfrom core.position_manager import PositionManagerprint('🚀 Quick End-to-End Test')engine = TradingEngine()book = OrderBook('AAPL')positions = PositionManager()# Test order creationorder_id = engine.create_order('AAPL', OrderSide.BUY, 100, OrderType.LIMIT, 150.0)print(f'✅ Created order: {order_id[:8]}...')# Test order bookorder = engine.orders[order_id]success = book.add_order_object(order)print(f'✅ Added to order book: {success}')# Test position tracking  positions.add_trade('AAPL', 50, 150.0)position = positions.get_position('AAPL')print(f'✅ Position: {position.quantity} shares @ ${position.average_price:.2f}')print('🎉 All core systems working!')
